{
  "name": "@cisco_open/mcpmock",
  "version": "1.0.0-rc1",
  "description": "MCP mock server toolkit - run, record, and build mock servers from mcpdesc files",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/cisco-open/mcptoolkit-mock.git"
  },
  "homepage": "https://github.com/cisco-open/mcptoolkit-mock#readme",
  "bugs": {
    "url": "https://github.com/cisco-open/mcptoolkit-mock/issues"
  },
  "main": "./build/index.js",
  "bin": {
    "mcpmock": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "npm run build && node build/index.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:watch": "node --experimental-vm-modules node_modules/jest/bin/jest.js --watch",
    "test:coverage": "node --experimental-vm-modules node_modules/jest/bin/jest.js --coverage",
    "clean": "rm -rf build",
    "test:links": "tests/check-doc-links.sh",
    "prerelease": "npm run test:links && npm run build && npm test"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "mock",
    "server",
    "testing",
    "development"
  ],
  "author": "stsfartz (Cisco DevNet)",
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^15.0.0",
    "handlebars": "^4.7.8",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.0",
    "@types/handlebars": "^4.1.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.0.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "js-yaml": "^4.2.0"
  }
}
